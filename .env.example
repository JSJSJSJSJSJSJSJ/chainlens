# Optional settings. Defaults work without credentials.
# Relative SQLite paths resolve from the current working directory.
# CHAINLENS_DATABASE_URL=sqlite:///./.runtime/chainlens.sqlite3
# CHAINLENS_SNAPSHOT=data/snapshots/2026-09-16.v1.json
# Frontend development proxy uses http://127.0.0.1:8000 by default.
